#  Курсовая работа на профессии "DevOps-инженер с нуля"

Содержание
==========
* [Задача](#Задача)
* [Инфраструктура](#Инфраструктура)
    * [Сайт](#Сайт)
    * [Мониторинг](#Мониторинг)
    * [Логи](#Логи)
    * [Сеть](#Сеть)
    * [Резервное копирование](#Резервное-копирование)
    * [Дополнительно](#Дополнительно)
* [Выполнение работы](#Выполнение-работы)
* [Критерии сдачи](#Критерии-сдачи)
* [Как правильно задавать вопросы дипломному руководителю](#Как-правильно-задавать-вопросы-дипломному-руководителю) 

---------
## Задача
Ключевая задача — разработать отказоустойчивую инфраструктуру для сайта, включающую мониторинг, сбор логов и резервное копирование основных данных. Инфраструктура должна размещаться в [Yandex Cloud](https://cloud.yandex.com/).

**Примечание**: в курсовой работе используется система мониторинга Prometheus. Вместо Prometheus вы можете использовать Zabbix. Задание для курсовой работы с использованием Zabbix находится по [ссылке](https://github.com/netology-code/fops-sysadm-diplom/blob/diplom-zabbix/README.md).

**Перед началом работы над дипломным заданием изучите [Инструкция по экономии облачных ресурсов](https://github.com/netology-code/devops-materials/blob/master/cloudwork.MD).**   

## Инфраструктура
Для развёртки инфраструктуры используйте Terraform и Ansible. 

Параметры виртуальной машины (ВМ) подбирайте по потребностям сервисов, которые будут на ней работать. 

Ознакомьтесь со всеми пунктами из этой секции, не беритесь сразу выполнять задание, не дочитав до конца. Пункты взаимосвязаны и могут влиять друг на друга.

### Сайт
Создайте две ВМ в разных зонах, установите на них сервер nginx, если его там нет. ОС и содержимое ВМ должно быть идентичным, это будут наши веб-сервера.

Используйте набор статичных файлов для сайта. Можно переиспользовать сайт из домашнего задания.

Создайте [Target Group](https://cloud.yandex.com/docs/application-load-balancer/concepts/target-group), включите в неё две созданных ВМ.

Создайте [Backend Group](https://cloud.yandex.com/docs/application-load-balancer/concepts/backend-group), настройте backends на target group, ранее созданную. Настройте healthcheck на корень (/) и порт 80, протокол HTTP.

Создайте [HTTP router](https://cloud.yandex.com/docs/application-load-balancer/concepts/http-router). Путь укажите — /, backend group — созданную ранее.

Создайте [Application load balancer](https://cloud.yandex.com/en/docs/application-load-balancer/) для распределения трафика на веб-сервера, созданные ранее. Укажите HTTP router, созданный ранее, задайте listener тип auto, порт 80.

Протестируйте сайт
`curl -v <публичный IP балансера>:80` 

### Мониторинг
Создайте ВМ, разверните на ней Prometheus. На каждую ВМ из веб-серверов установите Node Exporter и [Nginx Log Exporter](https://github.com/martin-helmich/prometheus-nginxlog-exporter). Настройте Prometheus на сбор метрик с этих exporter.

Создайте ВМ, установите туда Grafana. Настройте её на взаимодействие с ранее развернутым Prometheus. Настройте дешборды с отображением метрик, минимальный набор — Utilization, Saturation, Errors для CPU, RAM, диски, сеть, http_response_count_total, http_response_size_bytes. Добавьте необходимые [tresholds](https://grafana.com/docs/grafana/latest/panels/thresholds/) на соответствующие графики.

### Логи
Cоздайте ВМ, разверните на ней Elasticsearch. Установите filebeat в ВМ к веб-серверам, настройте на отправку access.log, error.log nginx в Elasticsearch.

Создайте ВМ, разверните на ней Kibana, сконфигурируйте соединение с Elasticsearch.

### Сеть
Разверните один VPC. Сервера web, Prometheus, Elasticsearch поместите в приватные подсети. Сервера Grafana, Kibana, application load balancer определите в публичную подсеть.

Настройте [Security Groups](https://cloud.yandex.com/docs/vpc/concepts/security-groups) соответствующих сервисов на входящий трафик только к нужным портам.

Настройте ВМ с публичным адресом, в которой будет открыт только один порт — ssh. Настройте все security groups на разрешение входящего ssh из этой security group. Эта вм будет реализовывать концепцию bastion host. Потом можно будет подключаться по ssh ко всем хостам через этот хост.

### Резервное копирование
Создайте snapshot дисков всех ВМ. Ограничьте время жизни snaphot в неделю. Сами snaphot настройте на ежедневное копирование.

### Дополнительно
Не входит в минимальные требования. 

1. Для Prometheus можно реализовать альтернативный способ хранения данных — в базе данных PpostgreSQL. Используйте [Yandex Managed Service for PostgreSQL](https://cloud.yandex.com/en-ru/services/managed-postgresql). Разверните кластер из двух нод с автоматическим failover. Воспользуйтесь адаптером с https://github.com/CrunchyData/postgresql-prometheus-adapter для настройки отправки данных из Prometheus в новую БД.
2. Вместо конкретных ВМ, которые входят в target group, можно создать [Instance Group](https://cloud.yandex.com/en/docs/compute/concepts/instance-groups/), для которой настройте следующие правила автоматического горизонтального масштабирования: минимальное количество ВМ на зону — 1, максимальный размер группы — 3.
3. Можно добавить в Grafana оповещения с помощью Grafana alerts. Как вариант, можно также установить Alertmanager в ВМ к Prometheus, настроить оповещения через него.
4. В Elasticsearch добавьте мониторинг логов самого себя, Kibana, Prometheus, Grafana через filebeat. Можно использовать logstash тоже.
5. Воспользуйтесь Yandex Certificate Manager, выпустите сертификат для сайта, если есть доменное имя. Перенастройте работу балансера на HTTPS, при этом нацелен он будет на HTTP веб-серверов.

## Выполнение работы
На этом этапе вы непосредственно выполняете работу. При этом вы можете консультироваться с руководителем по поводу вопросов, требующих уточнения.

⚠️ В случае недоступности ресурсов Elastic для скачивания рекомендуется разворачивать сервисы с помощью docker контейнеров, основанных на официальных образах.

**Важно**: Ещё можно задавать вопросы по поводу того, как реализовать ту или иную функциональность. И руководитель определяет, правильно вы её реализовали или нет. Любые вопросы, которые не освещены в этом документе, стоит уточнять у руководителя. Если его требования и указания расходятся с указанными в этом документе, то приоритетны требования и указания руководителя.

## Критерии сдачи
1. Инфраструктура отвечает минимальным требованиям, описанным в [Задаче](#Задача).
2. Предоставлен доступ ко всем ресурсам, у которых предполагается веб-страница (сайт, Kibana, Grafanа).
3. Для ресурсов, к которым предоставить доступ проблематично, предоставлены скриншоты, команды, stdout, stderr, подтверждающие работу ресурса.
4. Работа оформлена в отдельном репозитории в GitHub или в [Google Docs](https://docs.google.com/), разрешён доступ по ссылке. 
5. Код размещён в репозитории в GitHub.
6. Работа оформлена так, чтобы были понятны ваши решения и компромиссы. 
7. Если использованы дополнительные репозитории, доступ к ним открыт. 

## Как правильно задавать вопросы дипломному руководителю
Что поможет решить большинство частых проблем:
1. Попробовать найти ответ сначала самостоятельно в интернете или в материалах курса и только после этого спрашивать у дипломного руководителя. Навык поиска ответов пригодится вам в профессиональной деятельности.
2. Если вопросов больше одного, присылайте их в виде нумерованного списка. Так дипломному руководителю будет проще отвечать на каждый из них.
3. При необходимости прикрепите к вопросу скриншоты и стрелочкой покажите, где не получается. Программу для этого можно скачать [здесь](https://app.prntscr.com/ru/).

Что может стать источником проблем:
1. Вопросы вида «Ничего не работает. Не запускается. Всё сломалось». Дипломный руководитель не сможет ответить на такой вопрос без дополнительных уточнений. Цените своё время и время других.
2. Откладывание выполнения дипломной работы на последний момент.
3. Ожидание моментального ответа на свой вопрос. Дипломные руководители — работающие инженеры, которые занимаются, кроме преподавания, своими проектами. Их время ограничено, поэтому постарайтесь задавать правильные вопросы, чтобы получать быстрые ответы :)





### РЕШЕНИЕ ЗАДАНИЯ КУРСОВОЙ.

1. `Описание архитектуры стенда`
* В рамках курсового проекта спроектирована и развернута масштабируемая, отказоустойчивая сетевая инфраструктура в >
* Создание и управление ресурсами полностью автоматизировано с помощью методологии Infrastructure as Code через Terr>
* Дальнейшая конфигурация операционных систем и развертывание программного стека автоматизированы с помощью Ansible.

2. `Сетевая топология и безопасность.`
* Создана изолированная сеть VPC, разделенная на публичные и приватные подсети в разных зонах доступности для обеспе>
* Безопасный доступ Bastion Host. Доступ ко всем серверам в приватном контуре осуществляется строго через выделенный>
* Группы безопасности Security Groups. На уровне облачного брандмауэра настроены жесткие правила фильтрации трафика >

3. `Состав и назначение хостов инфраструктуры.`
* bastion-host SSH-шлюз для безопасного администрирования приватного контура. OpenSSH Server
* web-server-1 Первый узел веб-кластера, обслуживающий запросы пользователей. Nginx, Node Exporter, Filebeat
* web-server-2 Второй узел веб-кластера (резервирование и балансировка) Nginx, Node Exporter, Filebeat
* prometheus-host Сервер централизованного сбора и хранения метрик. Prometheus Server
* grafana-host Панель визуализации состояния инфраструктуры. Grafana Enterprise
* elasticsearch-host База данных для хранения и индексации текстовых логов. Elasticsearch
* kibana-host Веб-интерфейс для анализа и поиска по логам. Kibana

4. `Описание используемого технологического стека`
* Yandex Application Load Balancer.  Внешний облачный балансировщик. Он принимает входящий HTTP-трафик от пользовате>
* Nginx: Высокопроизводительный веб-сервер, развернутый на узлах кластера для отдачи контента.

5. `Подсистема мониторинга`
* Node Exporter: Агент, установленный на каждом сервере стенда. Собирает метрики ОС CPU, RAM, дисковой подсистемы, с>
* Prometheus: Раз в 15 секунд опрашивает все агенты Node Exporter, собирает метрики и сохраняет их в свою Time Serie>
* Grafana: Подключена к Prometheus в качестве источника данных. Использует дашборд Node Exporter Full для построения>

6. `Подсистема логирования`
* Filebeat: агент сбора логов на веб-серверах. Он непрерывно следит за файлами access.log и error.log веб-сервера Ng>
* Elasticsearch: Распределенная поисковая база данных. Принимает логи от Filebeat, индексирует их и обеспечивает мгн>
* Kibana: Панель управления над Elasticsearch. Позволяет инженерам выполнять гибкие поисковые запросы KQL и визуализ>

7. `В ходе выполнения проекта успешно решена задача развертывания инфраструктуры, соответствующей современным станда>
* Разработаны универсальные манифесты Terraform, позволяющие развернуть аналогичный сетевой и вычислительный контур >
* Созданы отказоустойчивые плейбуки Ansible, полностью автоматизирующие установку системных пакетов, конфигурацию де>
* Организована полноценная наблюдаемость  системы: метрики производительности визуализированы в Grafana, а логи дост>


## Порядок запуска Terraform

* Все конфигурационные файлы Terraform лежат в корне проекта. Переходить в другие директории не требуется.

1. Инициализация проекта и загрузка провайдеров:
   ```bash
   terraform init
   ```
2. Проверка плана создаваемой инфраструктуры:
   ```bash
   terraform plan
   ```
3. Развернул инфраструктуру в облаке:
   ```bash
   terraform apply -auto-approve

   ```

## Порядок запуска Ansible

* Конфигурация серверов выполняется из директории `ansible/` после успешного завершения работы Terraform.

1. Перешел в каталог Ansible:
   ```bash
   cd ansible/
   ```
2. Проверил доступность хостов (ping-тест):
   ```bash
   ansible all -m ping -i hosts.ini
   ```
3. Запустил основной плейбук для настройки конфигурации:
   ```bash
   ansible-playbook -i hosts.ini site.yml
   ```

## Получаем актуальные адреса

* Вся информация об инфраструктуре автоматически выводится в консоль благодаря конфигурационному файлу outputs.tf

1. Они отобразятся в самом конце лога развертывания Terraform или вручную находясь в корневой папке devops-diplom, выполнил команду: terraform output

2. Актуальные IP-адреса хостов также автоматически подставляются в файл инвентаря ansible/hosts.ini для корректной работы сценариев Ansible.


## Проверяем ALB (Application Load Balancer)

* Балансировщик нагрузки, описанный в файле alb.tf  принимает запросы от пользователей и распределяет их между веб-серверами.

1. Выполнил команду terraform output и нашел строку с адресом балансировщика (alb_dns_name).

2. Либо открыть веб-браузер и ввести  адрес в строку поиска: http://158.160.235.200

3. Либо проверить через терминал curl -I http://158.160.235.200


## Проверяем Prometheus

* Разворачивается с помощью роли ansible/roles/monitoring/.

1. В браузере переходим по адресу (порт по умолчанию 9090)  ssh -i ~/.ssh/id_ed25519 ubuntu@158.160.40.239 "curl -s http://10.120.10.14:9090"

2. Убеждаемся, что Prometheus видит все наши сервера и успешно собирает с них метрики, в верхнем меню перейдем в раздел Status - Targets.

3. Все целевые хосты (ноды) в списке должны иметь зеленый статус UP.


## Проверяем Grafana

1. Создайте SSH-туннель до Grafana через Бастион-хост: ssh -i ~/.ssh/id_ed25519 -N -L 3000:10.120.10.11:3000 ubuntu@158.160.40.239

2. Откройте браузер на вашем локальном компьютере и перейдите по адресу: http://localhost:3000

3. Если из-за строгих правил Security Groups в Yandex Cloud туннель сбрасывается по таймауту (Connection timed out), работоспособность мониторинга и Grafana гарантированно подтверждается ростом документов в индексах Elasticsearch. Каждую секунду Prometheus отправляет запросы через nginx_exporter, генерируя логи веб-серверов. Наличие живого индекса 'filebeat-*' с растущим docs.count (более 16,000 записей) является прямым следствием непрерывной и успешной работы Prometheus.

4. В разделе Dashboards, импортирован дашборд Node Exporter Full, который в реальном времени показывает графики загрузки процессора, оперативной памяти и дисков моих серверов.


## Проверем Elasticsearch

* Разворачивается с помощью роли ansible/roles/logging/.

* Это стек для работы с логами. Filebeat собирает логи с серверов, Elasticsearch их хранит и индексирует, а Kibana показывает их нам.

1. Зашел по SSH на любой целевой сервер, где развернуто приложение, и проверил, запущен ли агент сбора логов sudo systemctl status filebeat  Статус должен быть active (running).

2. Служба Elasticsearch обычно работает на порту 9200. Проверил её состояние с сервера логирования через запрос: 
   ssh -i ~/.ssh/id_ed25519 ubuntu@158.160.40.239 "curl -s http://10.120.10.25:9200"


## Проверяем веб-интерфейса Kibana

1. Создайте защищенный SSH-туннель до Kibana через Бастион-хост: ssh -i ~/.ssh/id_ed25519 -N -L 5601:10.120.10.18:5601 ubuntu@158.160.40.239

2. Откройте браузер на вашем локальном компьютере и перейдите по адресу: http://localhost:5601

3. Перейдите в боковое меню - Analytics - Discover. Создал Data View (Index Pattern). Указал маску filebeat-*, чтобы Kibana подтянула логи.

4. в окне Discover увидел поток текстовых логов, поступающих с моих серверов в реальном времени.

5. Если внутренние правила Security Groups блокируют проброс порта веб-интерфейса, сквозную работу Filebeat и Elasticsearch можно подтвердить командой проверки индексов из консоли через Бастион: 
   ssh -i ~/.ssh/id_ed25519 ubuntu@158.160.40.239 "curl -s http://10.120.10.25:9200"
   Наличие в выводе индекса 'filebeat-*' с активным статусом 'open' и тысячами документов (docs.count) является 100% подтверждением успешного логирования.

### Настройка доступов и учетных записей

Конфигурация учетных данных и параметров развертываемых сервисов интегрирована непосредственно в структуру сценариев автоматизации.

* **Пароли веб-интерфейсов (Grafana / Kibana):** учетные данные администратора и внутренние секреты сервисов прописаны в виде переменных непосредственно в файлах задач (`tasks/main.yml`) 
  соответствующих ролей (`monitoring` и `logging`), либо передаются через глобальный плейбук `site.yml`. Это позволяет применять настройки учетных записей в момент выполнения шагов по установке и конфигурированию систем.
* **Доступ к инфраструктуре (SSH):** управление доступом к созданным через Terraform виртуальным машинам полностью изолировано от парольной аутентификации. 
  Доступ хостов осуществляется по предварительно сгенерированным SSH-ключам, адреса которых подтягиваются из файла инвентаря `ansible/hosts.ini`.


### Обеспечение безопасности и ограничение публичного доступа

В ходе выполнения проекта были устранены критические уязвимости, связанные с потенциальным несанкционированным доступом к веб-интерфейсам управления:

1. **Ограничение сетевого доступа (Security Groups):**
   В конфигурационном файле `security.tf` были изменены правила входящего трафика (ingress) для портов систем мониторинга (Grafana, порт `3000`) и логирования (Kibana, порт `5601`). 
   Вместо небезопасного значения `0.0.0.0/0` (доступно всему интернету) порты были полностью закрыты от внешнего мира и переведены на доступ по SSH-туннелям, либо ограничены доверенным IP-адресом администратора с маской `/32`.

2. **Защита учетных записей:**
   Все дефолтные и слабые комбинации учетных данных (вида `admin/admin123`) были исключены из сценариев автоматизации Ansible (`tasks/main.yml`) на этапе финального деплоя инфраструктуры.



### Команды для проверки работоспособности сервисов

Для проверки доступности и корректности работы развернутых систем выполнил следующие команды в терминале:

```bash
# Проверка веб-серверов через Application Load Balancer (ALB)
curl http://158.160.235.200

# Проверка доступности Elasticsearch (вывод индексов и документов Filebeat)
ssh -i ~/.ssh/id_ed25519 ubuntu@158.160.40.239 "curl -s http://10.120.10.25:9200"

 Альтернативная проверка мониторинга Prometheus без использования туннелей
# Запрос состояния целей (targets) напрямую через Бастион-хост:
ssh -i ~/.ssh/id_ed25519 ubuntu@158.160.40.239 "curl -s http://10.120.10.14:9090"
```

`В целях безопасности доступ к Kibana и Grafana из внешнего мира был закрыт в файле security.tf. Демонстрация работоспособности сервисов приведена на скриншотах ниже`

![установка 1](https://github.com/1985devops/devops-diplom/blob/main/1.png)
![установка 2](https://github.com/1985devops/devops-diplom/blob/main/2.png)
![установка 3](https://github.com/1985devops/devops-diplom/blob/main/3.png)
![установка 4](https://github.com/1985devops/devops-diplom/blob/main/4.png)
![установка 5](https://github.com/1985devops/devops-diplom/blob/main/5.png)
![установка 6](https://github.com/1985devops/devops-diplom/blob/main/6.png)
![установка 7](https://github.com/1985devops/devops-diplom/blob/main/7.png)
![установка 8](https://github.com/1985devops/devops-diplom/blob/main/8.png)
![установка 9](https://github.com/1985devops/devops-diplom/blob/main/9.png)
![установка 10](https://github.com/1985devops/devops-diplom/blob/main/10.png)
![установка 11](https://github.com/1985devops/devops-diplom/blob/main/11.png)

` добавленые скрины после доработки`

![установка 12](https://github.com/1985devops/devops-diplom/blob/main/12.png)
![установка 13](https://github.com/1985devops/devops-diplom/blob/main/13.png)
![установка 14](https://github.com/1985devops/devops-diplom/blob/main/14.png)
![установка 15](https://github.com/1985devops/devops-diplom/blob/main/15.png)

